# 用户登录和注册功能使用说明

## 功能概述
抑郁症分析系统已添加独立的登录/注册页面，用户需要先登录才能访问系统功能。

## 页面结构

### 1. 登录页面（login.py）
- **访问地址**: http://localhost:8505（默认端口）
- **页面功能**: 用户登录和注册
- **页面布局**: 居中显示的登录框，包含登录和注册两个标签页

### 2. 主页面（app.py）
- **访问地址**: 登录后自动跳转
- **页面功能**: 抑郁症分析系统的所有功能
- **访问条件**: 必须先登录

## 使用流程

### 首次使用（注册）
1. 访问登录页面（login.py）
2. 点击"注册"标签页
3. 填写用户名、密码和确认密码
4. 点击"注册"按钮
5. 注册成功后自动跳转到主页面

### 已有账号（登录）
1. 访问登录页面（login.py）
2. 在"登录"标签页输入用户名和密码
3. 点击"登录"按钮
4. 登录成功后自动跳转到主页面

### 退出登录
1. 在主页面侧边栏点击"退出登录"按钮
2. 自动跳转回登录页面

## 功能访问权限
- **未登录状态**: 访问主页面会自动重定向到登录页面
- **已登录状态**: 可以正常使用所有功能（聚类相关、分类模型相关、预测模型相关、应用内可视化）

## 登录状态持久化
系统使用浏览器的 localStorage 保存登录状态：
- 页面刷新后会自动恢复登录状态
- 关闭浏览器后重新打开，仍然保持登录状态
- 只有点击"退出登录"才会清除登录状态

## 默认测试账号
系统预置了一个测试账号：
- **用户名**: admin
- **密码**: admin123

## 数据存储
用户数据保存在 `users.json` 文件中，格式为 JSON：
```json
{
  "用户名 1": "密码 1",
  "用户名 2": "密码 2"
}
```

## 文件说明
1. **login.py**: 登录页面，处理用户登录和注册
2. **app.py**: 主页面，包含所有抑郁症分析功能
3. **users.json**: 存储用户账号数据

## 运行方式

### 方式一：先运行登录页面（推荐）
```bash
streamlit run login.py
```
访问 http://localhost:8505 即可看到登录页面。

### 方式二：直接运行主页面
```bash
streamlit run app.py
```
如果未登录，会自动跳转到登录页面。

## 注意事项
1. 用户名不能为空
2. 密码不能为空
3. 注册时两次输入的密码必须一致
4. 用户名不能重复
5. 未登录时无法访问主页面功能
6. 登录状态保存在浏览器 localStorage 中，清除浏览器数据会导致需要重新登录
